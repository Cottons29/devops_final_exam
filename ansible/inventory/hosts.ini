[web]
# For local testing:  localhost ansible_connection=local
# For GitHub Actions: inventory is passed dynamically via CLI (-i <host>,)
# Uncomment below and set your server IP for manual runs:
# YOUR_SERVER_IP ansible_user=YOUR_USER ansible_ssh_private_key_file=~/.ssh/deploy_key
